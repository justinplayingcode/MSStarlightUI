import React from "react";
import "./index.scss"

interface UniformHeaderProps {
    
}

interface UniformHeaderState {
    
}

class UniformHeader extends React.Component<UniformHeaderProps, UniformHeaderState> {
    constructor(props: UniformHeaderProps) {
        super(props);
    }
    render() { 
        return ( 
            <div id="uniform-header">
                
            </div>
        );
    }
}

export default UniformHeader;